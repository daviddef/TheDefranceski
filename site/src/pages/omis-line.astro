---
import Base from "../layouts/Base.astro";
import Locator from "../components/Locator.astro";
import Spine from "../components/Spine.astro";
import spines from "../data/spines.json";
import Cluster from "../components/Cluster.astro";
import cl from "../data/clusters.json";
import Rule from "../components/Rule.astro";
import Sources from "../components/Sources.astro";
import o from "../data/omis.json";
const vmd = (x) => String(x ?? "")
  .replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>")
  .replace(/«(.+?)»/g, "<em>«$1»</em>")
  .replace(/(^|[^*])\*([^*\n]+?)\*/g, "$1<em>$2</em>");
import Plates from "../components/Plates.astro";
import Lightbox from "../components/Lightbox.astro";
---
<Base title="The Omiš Line" eyebrow="Seventeen generations published · 1318–1948"
      dek="A family chart for the Dalmatian branch, running from an envoy of Pope Urban V to the middle of the twentieth century. Three of its people were already in this archive under other sources, and they match."
>

  <section class="blk">
    <div class="lab">The line as ground covered</div>
    <h2 style="font-size:30px;margin:6px 0 4px">Seventeen generations, banded by what can be tested</h2>
    <Spine s={spines.spines.find(x => x.key === "venice")} />
  </section>

  <Locator place="Omiš" radius={52}
            note="Almissa. Eight households between 1639 and 1887 — and the town the family chart calls home from the seventeenth century." />


  <section class="blk">
    <div class="lab">And the parish underneath it</div>
    <h2 style="font-size:30px;margin:6px 0 4px">Eight households at Omiš, gathered independently of the chart</h2>
    <Cluster c={cl.clusters.dalmatia} />
  </section>

<section class="blk">
  <h2>{o.story.heading}</h2>
  <p class="dek" style="margin-top:8px">{o.story.lead}</p>
  <div style="margin-top:22px;display:flex;flex-direction:column;gap:16px">
    {o.story.rows.map(r => (
      <div class="one" style={r.key ? "border-left-color:var(--accent)" : ""}>
        <div class="lab">{r.lab}</div>
        <p style="margin-top:8px" set:html={r.text.replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/(^|[^*])\*([^*\n]+?)\*/g,"$1<em>$2</em>")}></p>
      </div>
    ))}
  </div>
  <p class="cap" style="margin-top:16px">{o.story.source_story}</p>
</section>

<Rule name="pomegranate" label="The branch itself" />

<section class="blk">
  <h2>{o.photos.heading}</h2>
  <p class="dek" style="margin-top:8px">{o.photos.lead}</p>
  <div style="margin-top:24px"><Plates ids={o.photos.ids} /></div>
</section>
<Lightbox />

<Rule name="arrows" label="The chart" />

<section class="blk">
  <div class="note">{o.caveat}</div>
  <div class="prose" style="margin-top:26px">
    <p>The chart is titled <em>Rodoslovno stablo obitelji Franceschi</em>. Read from the bottom it starts at Zadar — a notary, a municipal archivist, an officer of the port salt-house — moves to Omiš with an admiral, and then holds there as <em>posjednici</em>, landholders, for three hundred years.</p>
  </div>
</section>
<Rule name="arrows" />

<section class="blk">
  <h2>The direct line</h2>
  <div class="scroll" style="margin-top:22px">
    <table>
      <thead><tr><th>Generation</th><th class="m">Dates</th><th>What the chart records</th></tr></thead>
      <tbody>{o.line.map(([n,d,w],i)=>(
        <tr><td><span class="mono" style="color:var(--ink-3);margin-right:9px">{String(i+1).padStart(2,"0")}</span>{n}</td>
        <td class="m">{d}</td><td>{w}</td></tr>))}</tbody>
    </table>
  </div>
</section>
<Rule name="pomegranate" />

<section class="blk">
  <h2>Where it meets the rest of this archive</h2>
  <p class="dek">Six points where the chart and independently-found records touch. This is the test of a family tree: whether it survives contact with sources it was not drawn from.</p>
  <div class="stack" style="margin-top:26px">
    {o.matches.map(([n,t])=>(
      <div class="qrow">
        <div><h3 class="qq">{n}</h3></div>
        <div><p class="qw">{t}</p></div>
      </div>
    ))}
  </div>
</section>
<Rule name="helm" />

<section class="blk">
  <h2>The wider family on the chart</h2>
  <div class="scroll" style="margin-top:20px">
    <table><tbody>{o.siblings.map(([y,t])=>(
      <tr><td class="m" style="width:110px">{y}</td><td>{t}</td></tr>))}</tbody></table>
  </div>
  <div class="note" style="margin-top:26px">{o.omittedNote}</div>
  <p class="cap">{o.source}</p>
</section>
<Rule name="arrows" label="Almissa, 1840" />

<section class="blk">
  <h2>{o.gazzetta1840.heading}</h2>
  <p class="dek" style="margin-top:8px" set:html={vmd(o.gazzetta1840.lead)}></p>
  <div style="margin-top:22px;display:flex;flex-direction:column;gap:16px">
    {o.gazzetta1840.rows.map(r => (
      <div class="one" style={r.key ? "border-left-color:var(--accent)" : ""}>
        <div class="lab">{r.lab}</div>
        <p style="margin-top:8px;white-space:pre-line" set:html={r.text.replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>")}></p>
      </div>
    ))}
  </div>
  <p class="cap" style="margin-top:16px">{o.gazzetta1840.source_gdz}</p>
</section>

<Rule name="arrows" label="Zara — Široka ulica" />

<section class="blk">
  <h2>{o.zadarpalace.heading}</h2>
  <p class="dek" style="margin-top:8px" set:html={o.zadarpalace.lead.replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/\*(.+?)\*/g,"<em>$1</em>")}></p>
  <div style="margin-top:22px;display:flex;flex-direction:column;gap:16px">
    {o.zadarpalace.rows.map(r => (
      <div class="one" style={r.key ? "border-left-color:var(--accent)" : ""}>
        <div class="lab">{r.lab}</div>
        <p style="margin-top:8px;white-space:pre-line" set:html={r.text.replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/\*(.+?)\*/g,"<em>$1</em>")}></p>
      </div>
    ))}
  </div>
  <p class="cap" style="margin-top:16px">{o.zadarpalace.conf}</p>
</section>

  <Sources ids={["plemstvo", "omisphotos", "familysearch", "gazzetta-zara-1840"]} />
  <Rule name="arrows" label="A different kind of record" />
  <section class="blk">
    <h2 style="font-size:30px;margin:6px 0 10px">{o.valente.heading}</h2>
    <p class="dek" set:html={vmd(o.valente.lead)}></p>

    <blockquote class="vq" set:html={vmd(o.valente.quote)}></blockquote>
    <p class="vt" set:html={vmd(o.valente.trans)}></p>

    <div class="vwhy">
      {o.valente.why.map(w => <p set:html={vmd(w)}></p>)}
    </div>

    <p class="note" style="margin-top:20px" set:html={vmd(o.valente.where)}></p>
    <p class="note" style="margin-top:14px;border-left-color:var(--accent)" set:html={vmd(o.valente.next)}></p>
  </section>


</Base>

<style>
.vq{margin:20px 0 0;padding:14px 0 14px 20px;border-left:3px solid var(--ochre);
  font-family:var(--serif);font-size:16px;line-height:1.66;color:var(--ink-2);max-width:82ch}
.vt{font-family:var(--sans);font-size:14.5px;line-height:1.7;color:var(--ink);
  margin:16px 0 0;max-width:82ch}
.vwhy{margin-top:20px}
.vwhy p{font-family:var(--sans);font-size:14px;line-height:1.68;color:var(--ink-2);
  margin:0 0 14px;max-width:84ch}
</style>
